{
  "name": "final-project",
  "version": "1.0.0",
  "description": "Monorepo root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "npm start --workspace=client",
    "server": "npm start --workspace=server"
  }
}
